<Window x:Class="Ynost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ynost.Converters" 
        mc:Ignorable="d"
        Title="Ynost" Height="800" Width="1380"
        Loaded="MainWindow_Loaded"
        FontSize="16">

    <Window.Resources>
        <Style x:Key="WrappedTextBlockCellStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="4,2"/>
        </Style>
        <Style x:Key="EditingTextBoxStyle" TargetType="TextBox">
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="SpellCheck.IsEnabled" Value="True"/>
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="270" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="#f3f6ff">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- (0) НОВАЯ ПАНЕЛЬ-ЗАГОЛОВОК -->
                    <RowDefinition Height="Auto"/>
                    <!-- (1) Поиск -->
                    <RowDefinition Height="Auto"/>
                    <!-- (2) Кнопка "Учителя" -->
                    <RowDefinition Height="Auto"/>
                    <!-- (3) Разделитель (если нужен) -->
                    <RowDefinition Height="*"/>
                    <!-- (4) DataGrid -->
                    <RowDefinition Height="Auto"/>
                    <!-- (5) Кнопки Добавить/Удалить -->
                </Grid.RowDefinitions>

                <!-- (0) НОВАЯ ПАНЕЛЬ-ЗАГОЛОВОК -->
                <Border Grid.Row="0" Background="#E8EDF9" BorderBrush="#B0B0B0" BorderThickness="0,0,0,1" Padding="8,5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Текст "Сотрудники" теперь здесь, слева -->
                        <TextBlock Grid.Column="0" Text="{Binding StaffListTitle, FallbackValue='Преподаватели'}" 
                       FontWeight="Bold" VerticalAlignment="Center"/>

                        <!-- Кнопка теперь здесь, справа (пока без команды, вы можете привязать ее позже) -->
                        <!-- Например, можно использовать ее для экспорта всего списка преподавателей -->
                        <Button Grid.Column="1" Content="Экспорт в Excel" Padding="8,2" VerticalAlignment="Center" Command="{Binding ExportToExcelCommand}"/>
                    </Grid>
                </Border>

                <!-- (1) Поиск (старый Grid.Row="1" стал Grid.Row="1") -->
                <StackPanel Grid.Row="1" Margin="4,8,4,8">
                    <TextBlock Text="Поиск:" Margin="4,0,0,4"/>
                    <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" Margin="4,0"/>
                </StackPanel>

                <!-- (2) Кнопка "Учителя" (старый Grid.Row="2" стал Grid.Row="2") -->
                <Grid Grid.Row="2" Margin="4,0,4,8">
                    <Button Content="Учителя"
                Click="OnTeachersButton_Click"/>
                </Grid>

                <!-- (4) DataGrid (старый Grid.Row="3" стал Grid.Row="4") -->
                <DataGrid x:Name="TeachersGrid" Grid.Row="4" ItemsSource="{Binding Teachers}" SelectedItem="{Binding SelectedTeacher, Mode=TwoWay}" 
              AutoGenerateColumns="False" CanUserAddRows="False" Margin="4,0,4,0" HorizontalScrollBarVisibility="Auto"
              PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ФИО" Binding="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Width="*" 
                ElementStyle="{StaticResource WrappedTextBlockCellStyle}" 
                EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- (5) Кнопки Добавить/Удалить (старый Grid.Row="4" стал Grid.Row="5") -->
                <Grid Grid.Row="5" Margin="0,0,0,0" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Добавить" Command="{Binding AddStaffCommand}" Margin="0,8,0,8" Padding="15,2" Click="Button_Click_1"/>
                    <Button Grid.Column="1" Content="Удалить" Command="{Binding DeleteStaffCommand}" Margin="20,8,0,8" Padding="15,2"/>
                </Grid>
            </Grid>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#B0B0B0" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"/>

            <Grid Grid.Column="2">
                <TextBlock Text="{Binding StatusText}" FontSize="18" FontWeight="SemiBold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding IsLoadingOrSaving, Converter={StaticResource BoolToVisConverter}}"/>

                <TextBlock Text="Войдите в систему для показа данных" FontSize="18" FontWeight="SemiBold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding ShowLoginPrompt, Converter={StaticResource BoolToVisConverter}}"/>

                <ScrollViewer x:Name="DetailsScrollViewer" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                              Visibility="{Binding ShowDataContent, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel Margin="10" IsEnabled="{Binding CanEditData}">

                        <!-- 1. Итоговые результаты успеваемости (аттестат) -->
                        <TextBlock Text="1. Итоговые результаты успеваемости (аттестат)" FontWeight="Bold" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.AcademicResults}" SelectedItem="{Binding SelectedTeacher.SelectedAcademicResult, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left"
                                  IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}"
                                  PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Учебный год" Binding="{Binding AcademicPeriod}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="Auto" MinWidth="180" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ср. балл 1 сем." Binding="{Binding AvgSem1}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ср. балл 2 сем." Binding="{Binding AvgSem2}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Динамика сем." Binding="{Binding DynamicsSem}" IsReadOnly="True" Width="Auto" MinWidth="90" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <!--<DataGridTextColumn Header="Результаты промежуточной аттестации" Binding="{Binding ResultATest}" Width="Auto" MinWidth="300" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>-->
                                <DataGridTextColumn Header="Успеваемость 1 сем." Binding="{Binding AvgSuccessRate}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Успеваемость 2 сем." Binding="{Binding AvgSuccessRateSem2}" Width="Auto" MinWidth="160" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Динамика успев." Binding="{Binding DynamicsAvgSuccessRate}" IsReadOnly="True" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Качество 1 сем." Binding="{Binding AvgQualityRate}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Качество 2 сем." Binding="{Binding AvgQualityRateSem2}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Динамика кач." Binding="{Binding DynamicsAvgQualityRate}" IsReadOnly="True" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="СОУ Входной" Binding="{Binding EntrySouRate}" Width="Auto" MinWidth="70" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="СОУ Итоговый" Binding="{Binding ExitSouRate}" Width="Auto" MinWidth="70" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Динамика СОУ" Binding="{Binding DynamicsSouRate}" IsReadOnly="True" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddAcademicResultCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteAcademicResultCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 1A. Результаты промежуточной аттестации (НОВАЯ ТАБЛИЦА) -->
                        <TextBlock Text="1А. Результаты промежуточной аттестации" FontWeight="Bold" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.IntermediateAssessments}" SelectedItem="{Binding SelectedTeacher.SelectedIntermediateAssessment, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Учебный год" Binding="{Binding AcademicYear}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="Auto" MinWidth="180" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="СР БАЛ" Binding="{Binding AvgScore}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="КАЧЕСТВО" Binding="{Binding Quality}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="СОУ" Binding="{Binding Sou}" Width="Auto" MinWidth="90" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddIntermediateAssessmentCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteIntermediateAssessmentCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 2. Результаты государственной итоговой аттестации (ГИА) -->
                        <TextBlock Text="2. Результаты государственной итоговой аттестации (ГИА)" FontWeight="Bold" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.GiaResults}" SelectedItem="{Binding SelectedTeacher.SelectedGiaResult, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="Auto" MinWidth="160" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Группа" Binding="{Binding Group}" Width="Auto" MinWidth="80" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во участников" Binding="{Binding TotalParticipants}" Width="Auto" MinWidth="150" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «5»" Binding="{Binding Count5}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «4»" Binding="{Binding Count4}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «3»" Binding="{Binding Count3}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «2»" Binding="{Binding Count2}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Средний балл" Binding="{Binding AvgScore}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddGiaResultCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteGiaResultCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- Остальная часть кода без изменений -->
                        <!-- ... -->
                        <TextBlock Text="3. Результаты государственной аттестации (ДЭ)" FontWeight="Bold" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.DemoExamResults}" SelectedItem="{Binding SelectedTeacher.SelectedDemoExamResult, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Наименование компетенции" Binding="{Binding Subject}" Width="Auto" MinWidth="220" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Группа" Binding="{Binding Group}" Width="Auto" MinWidth="90" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во участников" Binding="{Binding TotalParticipants}" Width="Auto" MinWidth="150" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «5»" Binding="{Binding Count5}" Width="Auto" MinWidth="70" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «4»" Binding="{Binding Count4}" Width="Auto" MinWidth="70" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «3»" Binding="{Binding Count3}" Width="Auto" MinWidth="70" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во «2»" Binding="{Binding Count2}" Width="Auto" MinWidth="70" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Средний балл" Binding="{Binding AvgScore}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddDemoExamResultCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteDemoExamResultCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 4. Результаты освоения ... -->
                        <TextBlock Text="4. Результаты освоения обучающимися образовательных программ по итогам независимой оценки качества образования" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.IndependentAssessments}" SelectedItem="{Binding SelectedTeacher.SelectedIndependentAssessment, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Вид НОКО (организация)" Binding="{Binding AssessmentName}" Width="Auto" MinWidth="200" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding AssessmentDate}" Width="Auto" MinWidth="100" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Класс/Предмет" Binding="{Binding ClassSubject}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во всего" Binding="{Binding StudentsTotal}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во участвовавших" Binding="{Binding StudentsParticipated}" Width="Auto" MinWidth="180" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во справившихся" Binding="{Binding StudentsPassed}" Width="Auto" MinWidth="180" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во 5" Binding="{Binding Count5}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во 4" Binding="{Binding Count4}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во 3" Binding="{Binding Count3}" Width="Auto" MinWidth="90" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Кол-во 2" Binding="{Binding Count2}" Width="Auto" MinWidth="90" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddIndependentAssessmentCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteIndependentAssessmentCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 5. Деятельность по раннему самоопределению ... -->
                        <TextBlock Text="5. Деятельность по раннему самоопределению и профессиональной ориентации обучающихся" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.SelfDeterminations}" SelectedItem="{Binding SelectedTeacher.SelectedSelfDetermination, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Наименование мероприятия" Binding="{Binding Name}" Width="Auto" MinWidth="280" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Роль педагога" Binding="{Binding Role}" Width="Auto" MinWidth="160" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddSelfDeterminationCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteSelfDeterminationCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 6. Участие обучающихся в олимпиадах ... -->
                        <TextBlock Text="6. Участие обучающихся в олимпиадах, конкурсах, фестивалях, соревнованиях (далее – События)" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.StudentOlympiads}" SelectedItem="{Binding SelectedTeacher.SelectedStudentOlympiad, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Наименование События" Binding="{Binding Name}" Width="Auto" MinWidth="240" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Форма участия" Binding="{Binding Form}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="ФИО участника/кадета" Binding="{Binding Cadet}" Width="Auto" MinWidth="220" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Результат" Binding="{Binding Result}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddStudentOlympiadCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteStudentOlympiadCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 7. Деятельность в качестве члена жюри ... -->
                        <TextBlock Text="7. Деятельность в качестве члена жюри олимпиад, конкурсов" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.JuryActivities}" SelectedItem="{Binding SelectedTeacher.SelectedJuryActivity, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Наименование мероприятия" Binding="{Binding Name}" Width="Auto" MinWidth="280" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding EventDate}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddJuryActivityCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteJuryActivityCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 8. Проведение мастер-классов ... -->
                        <TextBlock Text="8. Проведение мастер-классов, открытых занятий, мероприятий" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.MasterClasses}" SelectedItem="{Binding SelectedTeacher.SelectedMasterClass, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="Auto" MinWidth="280" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding EventDate}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddMasterClassCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteMasterClassCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 9. Наличие выступлений ... -->
                        <TextBlock Text="9. Наличие выступлений на научно-практических конференциях (семинарах, педагогических чтениях, симпозиумах, круглых столах)" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.Speeches}" SelectedItem="{Binding SelectedTeacher.SelectedSpeech, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="Auto" MinWidth="280" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding EventDate}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddSpeechCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteSpeechCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 10. Наличие публикации ... -->
                        <TextBlock Text="10. Наличие публикации (наименование, где, когда опубликована, в том числе в электронном виде)" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.Publications}" SelectedItem="{Binding SelectedTeacher.SelectedPublication, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Наименование" Binding="{Binding Title}" Width="Auto" MinWidth="280" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddPublicationCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeletePublicationCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 11. Участие в экспериментальной ... -->
                        <TextBlock Text="11. Участие в экспериментальной и инновационной деятельности" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.ExperimentalProjects}" SelectedItem="{Binding SelectedTeacher.SelectedExperimentalProject, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Наименование проекта" Binding="{Binding Name}" Width="Auto" MinWidth="320" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddExperimentalProjectCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteExperimentalProjectCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 12. Наставническая деятельность -->
                        <TextBlock Text="12. Наставническая деятельность" FontWeight="Bold" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.Mentorships}" SelectedItem="{Binding SelectedTeacher.SelectedMentorship, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ФИО стажера" Binding="{Binding Trainee}" Width="Auto" MinWidth="220" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Номер приказа" Binding="{Binding OrderNo}" Width="Auto" MinWidth="160" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата приказа" Binding="{Binding OrderDate}" Width="Auto" MinWidth="140" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddMentorshipCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteMentorshipCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 13. Разработка программно-методического ... -->
                        <TextBlock Text="13. Разработка программно-методического сопровождения образовательного процесса" FontWeight="Bold" TextWrapping="Wrap" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.ProgramSupports}" SelectedItem="{Binding SelectedTeacher.SelectedProgramSupport, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Наименование рабочей программы" Binding="{Binding ProgramName}" Width="Auto" MinWidth="340" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridCheckBoxColumn Header="Контрольно-измерительные материалы" Binding="{Binding HasControlMaterials}" Width="Auto" MinWidth="260"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddProgramSupportCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteProgramSupportCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                        <!-- 14. Участие в профессиональных конкурсах -->
                        <TextBlock Text="14. Участие в профессиональных конкурсах" FontWeight="Bold" Margin="0,8,0,4"/>
                        <DataGrid ItemsSource="{Binding SelectedTeacher.ProfessionalCompetitions}" SelectedItem="{Binding SelectedTeacher.SelectedProfessionalCompetition, Mode=TwoWay}" PreviewMouseWheel="DataGrid_PreviewMouseWheel" AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False" HorizontalScrollBarVisibility="Auto" MinRowHeight="25" HorizontalAlignment="Left" IsReadOnly="{Binding Path=DataContext.CanEditData, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}" PreparingCellForEdit="DataGrid_PreparingCellForEdit" CellEditEnding="DataGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="Auto" MinWidth="130" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Наименование конкурса" Binding="{Binding Name}" Width="Auto" MinWidth="240" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Достижение" Binding="{Binding Achievement}" Width="Auto" MinWidth="160" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Дата" Binding="{Binding EventDate}" Width="Auto" MinWidth="110" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                                <DataGridTextColumn Header="Ссылка" Binding="{Binding Link}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource WrappedTextBlockCellStyle}" EditingElementStyle="{StaticResource EditingTextBoxStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,12">
                            <Button Content="+" Command="{Binding SelectedTeacher.AddProfessionalCompetitionCommand}" Padding="5,0" MinWidth="30" MinHeight="25"/>
                            <Button Content="-" Command="{Binding SelectedTeacher.DeleteProfessionalCompetitionCommand}" Margin="5,0,0,0" Padding="5,0" MinWidth="30" MinHeight="25"/>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <Border Grid.Row="1" Background="#FFE0E0E0" Padding="5" BorderBrush="Gray" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="2" Content="Сохранить" HorizontalAlignment="Left" Padding="10,3" Command="{Binding SaveChangesCommand}"/>
                <!-- Кнопка Сохранить теперь слева -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding LoginButtonText}" Padding="10,3" Command="{Binding ToggleLoginCommand}"/>
                    <TextBlock Text="{Binding UserStatusText}" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"
                               Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="LightGray" BorderBrush="Gray" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding ConnectionStatusText}" Padding="5" VerticalAlignment="Center" TextWrapping="Wrap"/>
                <Button Grid.Column="1" Content="Повторить попытку" Margin="0,0,5,0" Padding="10,3" VerticalAlignment="Center"
                        Command="{Binding RetryConnectionCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsLoggedIn}" Value="True"/>
                                        <Condition Binding="{Binding IsDatabaseConnected}" Value="False"/>
                                        <Condition Binding="{Binding IsLoading}" Value="False"/>
                                        <Condition Binding="{Binding IsSavingData}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>